export const POLLING_INTERVAL_MS = 60_000;

export const LOCAL_STORAGE_KEYS = {
  TOKEN: 'token',
  VIEWER_USERNAME: 'viewerUsername',
} as const;

export const GITHUB_API_URL = 'https://api.github.com/graphql';

export const TIMELINE_FETCH_LIMIT = 100;
export const SEARCH_RESULTS_LIMIT = 100;
